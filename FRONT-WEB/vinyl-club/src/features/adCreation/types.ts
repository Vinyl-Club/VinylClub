 
export type State = {
    fieldErrors: Record<string, string>;
    formError: string;
};

export type Category = {
  id: number;
  name: string;
};

export type Artist = {
  id: number;
  name: string;
};

export type Album = {
  id: number;
  name: string;
};
